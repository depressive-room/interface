project interface
	: requirements <cxxflags>-fPIC <threading>multi <include>../render/src
	: default-build <variant>release
	: build-dir build
	;

use-project render : ../render ;

build-project src ;
